# Stripe (mode TEST recommandé pour dev)
# Récupère la clé secrète dans Stripe Dashboard → Developers → API keys
STRIPE_SECRET_KEY=YOUR_STRIPE_SECRET_KEY_HERE

# Webhook Stripe (signature)
# Récupère le "Signing secret" dans Stripe Dashboard → Developers → Webhooks
STRIPE_WEBHOOK_SECRET=YOUR_STRIPE_WEBHOOK_SECRET_HERE

# Optionnel: force l’URL de base utilisée pour les redirects (utile en local)
# SITE_URL=http://localhost:8888

# Google Sheets (optionnel)
# 1) Crée un Google Sheet et récupère son ID
# 2) Crée un Service Account (Google Cloud) + partage le Sheet au client email
GOOGLE_SHEETS_ID=YOUR_GOOGLE_SHEETS_ID
GOOGLE_SHEETS_TAB=Orders
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=YOUR_PRIVATE_KEY

# Notifications (optionnel)
# Exemple: Discord/Slack webhook URL (ou un webhook Make/Zapier)
ORDER_NOTIFICATION_WEBHOOK_URL=YOUR_WEBHOOK_URL
