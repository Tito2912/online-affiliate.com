{
  "name": "online-affiliate.com",
  "version": "1.0.0",
  "description": "Site statique prêt à déployer sur **Netlify** : landing + configurateur (prix en temps réel) + parcours de **commande** avec page de paiement (Stripe via Netlify Functions).",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "indexnow:submit": "node scripts/indexnow-submit.mjs",
    "netlify:dev": "node scripts/netlify-dev.mjs",
    "sheets:create": "node scripts/create-google-sheet.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "tsparticles-slim": "^2.12.0"
  }
}
